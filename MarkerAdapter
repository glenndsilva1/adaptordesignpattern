class MarkerAdapter implements Pen {

    Marker marker;

    public MarkerAdapter(Marker marker) {
        this.marker = marker;
    }

    @Override
    public void write() {
        marker.mark();
    }
}
